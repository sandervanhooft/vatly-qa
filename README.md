# Vatly QA Assets

Screenshots and test artifacts for Vatly PR reviews.

**Structure:**
```
screenshots/
  pr-{number}/
    {description}.png
```

**Usage:** Assets are uploaded via the `gh` CLI and referenced in PR comments.

This repo is public so images can be embedded in GitHub comments.
